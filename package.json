{
  "name": "minor_proj",
  "private": true,
  "description": "A.E.G.I.S auth — MySQL + Express API + Vite client",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix visualization",
    "install:all": "npm install --prefix server && npm install --prefix visualization"
  }
}
